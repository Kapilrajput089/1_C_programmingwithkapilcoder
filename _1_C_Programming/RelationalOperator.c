#include"stdio.h"
#include"conio.h"
int main(){

       printf("Relational Operators in C programming\n");
       // Relational Operators are :-(<, >, <=, >=, ==, !=).
       int a=5,b=6;
       printf("1. Is A > B : %d\n", (a> b));
       printf("2. Is A < B : %d\n", (a< b));
       printf("3. Is A <= B : %d\n", (a<= b));
       printf("3. Is A >= B : %d\n", (a>= b));
       printf("3. Is A == B : %d\n", (a== b));
       printf("3. Is A != B : %d\n", (a!= b));
       getch();
       return 0;
}