// write a program to use Arithmetic operator in C with kapilcoder.
#include"stdio.h"
#include"conio.h"
int main()
{
    printf("Arithmeic Operator in C programming\n");
     // Arithmetic operators are :- (+, -, *, /, %).
    int a=70,b=40;
    printf("Addition of A + B = %d\n ",(a+b));
    printf("Subtraction of A - B =  %d\n",(a-b));
    printf("Multiplication of A * B = %d\n",(a*b));
    printf("Division of  A / B = %d\n",(a/b));
    printf("Modulus of A %% B = %d",(a% b));
    // These all are  Arithetic operators in C programming.
    getch();
    return 0;

}